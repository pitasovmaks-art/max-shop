# Точка Монтажа — статус проекта

Мини-приложение для Max Messenger. Магазин строительных инструментов с каталогом, корзиной и оформлением заказа.

---

## Стек

- **Frontend**: Vanilla JS, HTML/CSS (без фреймворков)
- **Backend**: Node.js + Express
- **БД**: PostgreSQL (хостинг Timeweb, SSL `rejectUnauthorized: false`)
- **Хранилище**: S3-совместимое (Timeweb, endpoint `s3.twcstorage.ru`)
- **Деплой**: Railway
- **Платформа**: Max Messenger Mini App SDK

---

## Реализовано

### Каталог (`index.html`, `src/catalog/`)
- Загрузка товаров, категорий и подкатегорий из API
- Фильтрация по категории и подкатегории
- Поиск с debounce 250 мс
- Двухколоночная сетка карточек, карточки своей естественной высоты
- Бейджи «Нет в наличии» и название подкатегории на фото
- Варианты товара (пилюли) с выбором и обновлением цены
- Клик на карточку открывает страницу товара

### Страница товара (`src/catalog/product.html` + `product.js`)
- Загрузка товара и дополнительных фото из API
- Свайп-галерея с touch-событиями (порог 50px, защита от вертикального скролла)
- Точки-индикаторы под главным фото
- Стрип миниатюр для переключения фото
- Варианты товара с выбором цены
- Кнопка «В корзину» / «Записаться» / «Нет в наличии»
- Кнопка назад: `history.back()` или главная

### Корзина (`cart.html`, `src/cart/`)
- Список товаров с количеством и итоговой суммой
- Изменение количества и удаление позиций
- Бейджи с количеством товаров в шапке и нижней навигации
- Хранение в `localStorage`, ключ `productId` или `productId_v{variantId}`

### Оформление заказа (`checkout.html`, `src/cart/`)
- Форма с именем, телефоном, адресом и комментарием
- Отправка заказа в API
- Уведомление администратора через бота Max Messenger

### Бот Max Messenger (`bot.js`)
- Long polling на `platform-api.max.ru`
- Команда `/start` — кнопка открытия мини-приложения
- Уведомления о новых заказах в чат администратора

### Админ-панель (`admin/`)
- Авторизация через JWT (токен в `sessionStorage`)
- CRUD товаров: название, описание, цена, категория, подкатегория, наличие, метка услуги
- Загрузка главного фото товара в S3
- Управление дополнительными фото (загрузка/удаление, хранятся в `product_images`)
- CRUD категорий и подкатегорий
- Просмотр и управление заказами
- Управление точками выдачи (stores)

### Backend API (`server/`)
- `GET/POST/PUT/DELETE /api/products`
- `GET /api/products/:id`
- `GET/POST/DELETE /api/products/:id/images`
- `GET/POST/PUT/DELETE /api/categories`
- `GET/POST/PUT/DELETE /api/subcategories`
- `GET/POST/PUT /api/orders`
- `GET/POST/PUT/DELETE /api/stores`
- `POST /api/upload` — загрузка файла в S3, возвращает публичный URL
- `POST /api/auth/login` — выдача JWT

### БД (`server/db.js`)
- Таблицы: `products`, `categories`, `subcategories`, `orders`, `stores`, `product_images`
- Авто-создание схемы при старте (`createSchema`)
- Авто-сид товаров при пустой таблице

### Безопасность
- `X-Frame-Options: ALLOWALL` и `Content-Security-Policy: frame-ancestors *` для работы в iframe Max
- JWT middleware для защиты admin-эндпоинтов

### Юридические документы
- `offer.html` — публичная оферта (ст. 492 ГК РФ), дата 25 мая 2026 г.
- `privacy.html` — политика конфиденциальности, дата 25 мая 2026 г.

### Дизайн — тёмная тема с белым акцентом (25 мая 2026)
- Единая цветовая палитра по всему проекту: `#141414` фон страницы, `#1E1E1E` карточки, `#FFFFFF` акцент (кнопки, активные пилюли, бейджи), `#666666` вторичный текст, `#2A2A2A` рамки
- Переписаны: `catalog.css`, `cart.css`, `checkout.css`, `admin.css`, `cookie.css`, inline-стили в `product.html`
- Все кнопки действий (корзина, оформить, сохранить, войти, FAB) — белые с чёрным текстом
- Логотип в шапке каталога заменён с `<img>` на текст «Точка Монтажа»
- Фон `body` во всех HTML-файлах выставлен в `#141414` для предотвращения мигания при загрузке

---

## На чём остановились

Дизайн приведён к единому виду. Функциональность не менялась. Следующий логичный шаг — проверить приложение на реальном устройстве в Max Messenger и при необходимости скорректировать отступы или шрифты.

---

## Планируется

- Тестирование на реальных устройствах в Mini App окружении Max
- Поиск по штрихкоду / артикулу
- Избранные товары (wishlist)
- История заказов для пользователя
- Push-уведомления о статусе заказа через бота
- Пагинация или виртуальный скролл при большом каталоге
- Сортировка товаров (по цене, новизне)
- Промокоды и скидки
- Загрузка нескольких фото одновременно в админке
